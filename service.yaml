name: distance
author: vu-ase
source: SERVICE_SOURCE
version: 1.0.1
description: The distance service

commands:
  build: make build
  run: ./bin/distance

inputs: []

outputs: 
  - distance-m

configuration: 
  - name: polling-delay
    type: number
    value: 20 # ms
    tunable: false
